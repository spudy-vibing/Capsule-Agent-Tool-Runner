## OUTPUT FORMAT - READ CAREFULLY

Your response must be EXACTLY ONE of these JSON formats. Copy the format exactly.

TO LIST FILES:
{"tool": "shell.run", "args": {"cmd": ["ls", "TARGET_DIR"]}}

TO READ A FILE:
{"tool": "fs.read", "args": {"path": "TARGET_DIR/filename.txt"}}

WHEN FINISHED:
{"done": true, "reason": "task_complete", "output": {"files_scanned": 2, "findings": []}}

WRONG (do not do this):
- {"tool": "shell.run", "args": {"cmd": ["find", ...]}}  <-- NO FIND COMMAND
- Any Python code or expressions
- Any string concatenation with +
- Any .join() or other methods

## YOUR TASK

Directory to scan: {{ input.target_directory }}
Look for: AWS keys starting with AKIA, passwords, private keys

## WHAT TO DO

Step 1: Output exactly: {"tool": "shell.run", "args": {"cmd": ["ls", "{{ input.target_directory }}"]}}
Step 2: For each .md or .txt file, read it with fs.read
Step 3: After reading all files, output done with findings
